# Environment Variables for Production
# Copy this file to .env and set your own values

# Flask Secret Key - REQUIRED for production
# Generate a secure key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secure-random-key-here-change-this

# Optional: Flask Environment (development/production)
# FLASK_ENV=production

# Optional: Flask Debug Mode (0 for production, 1 for development)
# FLASK_DEBUG=0

# ============================================================
# PROXY CONFIGURATION (for preventing IP bans)
# ============================================================

# Single Proxy (uncomment and set if you have one proxy)
# Format: protocol://host:port or protocol://username:password@host:port
# SCRAPER_PROXY=http://proxy.example.com:8080
# SCRAPER_PROXY=http://username:password@premium-proxy.com:8080

# Multiple Proxies (comma-separated, uncomment and set if you have multiple)
# SCRAPER_PROXIES=http://proxy1.com:8080,http://user:pass@proxy2.com:3128,http://proxy3.com:8080

# Examples for popular proxy services:

# BrightData (Luminati)
# SCRAPER_PROXY=http://username-session-random:password@zproxy.lum-superproxy.io:22225

# Oxylabs
# SCRAPER_PROXY=http://customer-username:password@pr.oxylabs.io:7777

# ScraperAPI
# SCRAPER_PROXY=http://scraperapi:YOUR_API_KEY@proxy-server.scraperapi.com:8001

# Note: 
# - User-agent rotation works automatically without proxies
# - Proxies are optional but recommended for production
# - Free proxies are unreliable - use paid services for best results
# - See PROXY_ROTATION_GUIDE.md for detailed setup instructions

# Note: Never commit the actual .env file to version control
# The .env file is already included in .gitignore
