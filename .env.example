# Environment Variables for Production
# Copy this file to .env and set your own values

# Flask Secret Key - REQUIRED for production
# Generate a secure key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secure-random-key-here-change-this

# Optional: Flask Environment (development/production)
# FLASK_ENV=production

# Optional: Flask Debug Mode (0 for production, 1 for development)
# FLASK_DEBUG=0

# Note: Never commit the actual .env file to version control
# The .env file is already included in .gitignore
